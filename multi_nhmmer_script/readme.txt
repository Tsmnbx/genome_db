multiple nhmmer .py ()
    
    -goes through all the files in the current directory that the script is ran, 
    -finds which ones are .hmm files, and which are .gbff.gz files
    -search each .hmm file against every .gbff.gz file
    -saves output in the current directory in tblout format
    
get info .py	(name of target file to be parsed)
    
    -looks at the target file specified
    -get info
    -saves the info acquired in new text file with the same name as the target file with the prefix "parsed_"
    
    
feel free to change the codes